<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="8dp"
        android:paddingBottom="24dp">

        <!-- Active word color -->
        <LinearLayout style="@style/ColorRow">
            <TextView style="@style/ColorRowLabel" android:text="Active word color" />
            <View android:id="@+id/swatchActiveWordColor" style="@style/ColorSwatch" />
            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchActiveWordColor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <!-- Active word background -->
        <LinearLayout style="@style/ColorRow">
            <TextView style="@style/ColorRowLabel" android:text="Active word background" />
            <View android:id="@+id/swatchActiveWordBg" style="@style/ColorSwatch" />
            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchActiveWordBg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <TextView style="@style/ColorRowLabel" android:layout_marginTop="6dp" android:text="Background corner radius" />
        <SeekBar
            android:id="@+id/seekActiveWordBgRadius"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="24" />

        <!-- Stroke -->
        <LinearLayout style="@style/ColorRow" android:layout_marginTop="10dp">
            <TextView style="@style/ColorRowLabel" android:text="Stroke" />
            <View android:id="@+id/swatchStroke" style="@style/ColorSwatch" />
            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchStroke"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <!-- Shadow -->
        <LinearLayout style="@style/ColorRow" android:layout_marginTop="10dp">
            <TextView style="@style/ColorRowLabel" android:text="Shadow" />
            <View android:id="@+id/swatchShadow" style="@style/ColorSwatch" />
            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchShadow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/chipShadowDown"
                style="@style/BreakChip"
                android:text="Down" />

            <TextView
                android:id="@+id/chipShadowRight"
                style="@style/BreakChip"
                android:text="Right" />
        </LinearLayout>

        <!-- Caption background -->
        <LinearLayout style="@style/ColorRow" android:layout_marginTop="10dp">
            <TextView style="@style/ColorRowLabel" android:text="Caption background" />
            <View android:id="@+id/swatchCaptionBg" style="@style/ColorSwatch" />
            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/switchCaptionBg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>
