<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="8dp"
        android:paddingBottom="24dp">

        <TextView style="@style/ColorRowLabel" android:text="Line breaks" />
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:orientation="horizontal">
            <TextView android:id="@+id/chipBreakPunctuation" style="@style/BreakChip" android:text="Punctuation / pause" />
            <TextView android:id="@+id/chipBreakSingleWord" style="@style/BreakChip" android:text="Single word" />
            <TextView android:id="@+id/chipBreakRandom" style="@style/BreakChip" android:text="Random" />
        </LinearLayout>

        <TextView style="@style/ColorRowLabel" android:layout_marginTop="20dp" android:text="Page breaks (lines shown at once)" />
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:orientation="horizontal">
            <TextView android:id="@+id/chipPage1" style="@style/BreakChip" android:text="1" />
            <TextView android:id="@+id/chipPage2" style="@style/BreakChip" android:text="2" />
            <TextView android:id="@+id/chipPage3" style="@style/BreakChip" android:text="3" />
            <TextView android:id="@+id/chipPage4" style="@style/BreakChip" android:text="4" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>
